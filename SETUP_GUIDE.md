# מדריך התקנה למפתחים חדשים

מדריך זה מיועד למי שזו הפעם הראשונה שלו עובד עם GitHub ופרויקט קוד משותף.
עקוב אחרי השלבים בזהירות - כל שלב חשוב!

---

## שלב 1: התקנת התוכנות הנדרשות

### 1.1 התקנת Node.js

Node.js זה מה שמריץ את הקוד שלנו.

1. לך לאתר: https://nodejs.org/
2. הורד את הגרסה **LTS** (הכפתור הירוק בצד שמאל)
3. הפעל את הקובץ שירד ועקוב אחרי ההוראות (לחץ Next בכל מסך)
4. **חשוב:** סמן את האופציה "Add to PATH" אם היא מופיעה

**לבדוק שההתקנה הצליחה:**
פתח Terminal (ב-Mac) או Command Prompt (ב-Windows) והקלד:
```bash
node --version
```
אם הצליח, תראה מספר גרסה כמו `v20.10.0`

### 1.2 התקנת Git

Git זה הכלי שמאפשר לנו לעבוד ביחד על אותו קוד.

1. לך לאתר: https://git-scm.com/
2. הורד את הגרסה למערכת ההפעלה שלך
3. הפעל את הקובץ שירד ועקוב אחרי ההוראות (ברירות המחדל בסדר)

**לבדוק שההתקנה הצליחה:**
```bash
git --version
```
אם הצליח, תראה מספר גרסה כמו `git version 2.43.0`

### 1.3 התקנת VS Code (עורך הקוד)

1. לך לאתר: https://code.visualstudio.com/
2. הורד והתקן
3. פתח את VS Code אחרי ההתקנה

---

## שלב 2: יצירת חשבון GitHub

1. לך לאתר: https://github.com/
2. לחץ על **Sign up**
3. הזן אימייל, סיסמה ושם משתמש
4. אשר את האימייל שיגיע אליך

---

## שלב 3: הגדרת Git במחשב שלך

פתח Terminal ב-VS Code (לחץ `Ctrl+`` או View > Terminal) והקלד:

```bash
git config --global user.name "השם שלך באנגלית"
git config --global user.email "האימייל שלך ב-GitHub"
```

לדוגמה:
```bash
git config --global user.name "Yossi Cohen"
git config --global user.email "yossi@example.com"
```

---

## שלב 4: הורדת הפרויקט למחשב

### 4.1 בחר תיקייה לפרויקטים

פתח Terminal והקלד (או בחר תיקייה אחרת שמתאימה לך):

**ב-Mac:**
```bash
cd ~/Documents
mkdir Projects
cd Projects
```

**ב-Windows:**
```bash
cd %USERPROFILE%\Documents
mkdir Projects
cd Projects
```

### 4.2 שיבוט (Clone) הפרויקט

```bash
git clone <הכנס כאן את הלינק שתקבל מהמפתח הראשי>
cd gidi-inventory
```

---

## שלב 5: התקנת תלויות הפרויקט

עכשיו שהפרויקט במחשב, צריך להתקין את כל הספריות שהוא צריך:

```bash
npm install
```

זה יקח כמה דקות. חכה עד שזה יסיים.

---

## שלב 6: הפעלת הפרויקט

```bash
npm run dev
```

אם הכל עבד, תראה הודעה כזו:
```
  VITE v6.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**פתח את הדפדפן** ולך ל: http://localhost:5173

תראה את האפליקציה רצה! 🎉

---

## שלב 7: פתיחת הפרויקט ב-VS Code

1. ב-VS Code, לחץ File > Open Folder
2. נווט לתיקיית הפרויקט ופתח אותה
3. עכשיו אתה יכול לראות ולערוך את כל הקבצים

---

## עבודה יומיומית

### לפני שמתחילים לעבוד - עדכון הקוד

תמיד לפני שמתחילים לעבוד, משכו את השינויים האחרונים:
```bash
git pull
```

### אחרי שעשית שינויים - שמירה ושליחה

```bash
# לראות מה השתנה
git status

# להוסיף את כל השינויים
git add .

# לשמור עם הודעה שמסבירה מה עשית
git commit -m "הסבר קצר על השינוי"

# לשלוח ל-GitHub
git push
```

### אם יש בעיה

אם משהו לא עובד, נסה:
```bash
# למחוק את התלויות ולהתקין מחדש
rm -rf node_modules
npm install

# להפעיל מחדש
npm run dev
```

---

## פקודות שימושיות

| פקודה | מה היא עושה |
|-------|-------------|
| `npm run dev` | מפעיל את השרת לפיתוח |
| `npm run build` | בונה גרסה לייצור |
| `npm run lint` | בודק שגיאות בקוד |
| `git status` | מראה מה השתנה |
| `git pull` | מושך שינויים מ-GitHub |
| `git push` | שולח שינויים ל-GitHub |

---

## צריך עזרה?

אם משהו לא עובד, צלם מסך של השגיאה ושלח למפתח הראשי.
